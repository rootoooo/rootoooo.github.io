addEventListener('fetch', event => {
  event.respondWith(handleRequest(event.request));
});

async function handleRequest(request) {
  const ip = request.headers.get('CF-Connecting-IP') || '未知';
  const country = request.headers.get('CF-IPCountry') || '未知';
  const ChatURL = 'https://chat.haoss.eu.org';
  const PansouURL = 'https://so.252035.xyz';
  const MusicURL = 'https://music.gdstudio.xyz';
  const BilibiliURL = 'https://bilibili.com';
  const EconomyURL = 'https://zh.tradingeconomics.com';

  const html = `
    <!DOCTYPE html>
    <html lang="zh">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>皮皮虾</title>
      <link rel="icon" type="image/jpg" href="https://haoss.eu.org/ppx.jpg" />
      <style>
        body {
          font-family: Arial, sans-serif;
          text-align: center;
          margin: 0;
          padding: 0;
          height: 100vh;
          display: flex;
          justify-content: center;
          align-items: center;
          background: url('https://cf.haoss.eu.org/oooo/https/raw.githubusercontent.com/rootoooo/rootoooo.github.io/refs/heads/main/ppx01.jpg') no-repeat center center fixed;
          background-size: cover;
        }
        .container {
          background: rgba(255, 255, 255, 0.7);
          padding: 20px;
          border-radius: 10px;
          box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
          text-align: center;
        }
        h1 {
          font-size: 24px;
          margin-bottom: 20px;
        }
        p {
          font-size: 18px;
          margin: 10px 0;
        }
        .link-container {
          margin-top: 10px;
          display: flex;
          justify-content: center;
          gap: 15px;
          flex-wrap: wrap;
        }
        .link-container a {
          font-size: 16px;
          color: blue;
          text-decoration: underline;
          cursor: pointer;
        }
        .input-container,
        .video-input-container,
        .translate-input-container,
        .startpage-input-container,
        .content-input-container {
          margin-top: 20px;
        }
        input {
          font-size: 16px;
          padding: 8px;
          width: 200px;
          margin-right: 10px;
        }
        button {
          font-size: 16px;
          padding: 8px 16px;
          cursor: pointer;
        }
      </style>
    </head>
    <body>
      <div class="container">
        <h1>There is everything in dreams</h1>
        <p><strong>IP:</strong> ${ip}</p>
        <p><strong>Nation:</strong> ${country}</p>
        <div class="link-container">
          <a href="${ChatURL}" class="details-link">Chat</a>
          <a href="${PansouURL}" class="details-link">Pansou</a>
          <a href="${MusicURL}" class="details-link">Music</a>
          <a href="${BilibiliURL}" class="details-link">Bilibili</a>
          <a href="${EconomyURL}" class="details-link">Economy</a>
        </div>
        <div class="input-container">
          <input type="text" id="ip-input" placeholder="IP Port" />
          <button onclick="goToPing()">Go</button>
        </div>
        <div class="video-input-container">
          <input type="text" id="video-input" placeholder="Video" />
          <button onclick="goToVideo()">Go</button>
        </div>
        <div class="translate-input-container">
          <input type="text" id="translate-input" placeholder="Translate" />
          <button onclick="goToTranslate()">Go</button>
        </div>
        <div class="startpage-input-container">
          <input type="text" id="startpage-input" placeholder="Startpage" />
          <button onclick="goToStartpage()">Go</button>
        </div>
        <div class="content-input-container">
          <input type="text" id="content-input" placeholder="Content" />
          <button onclick="goToContent()">Go</button>
        </div>
      </div>

      <script>
        function goToPing() {
          const ip = document.getElementById('ip-input').value;
          if (ip) {
            window.location.href = 'https://ping.pe/' + ip;
          } else {
            alert('IP Port');
          }
        }

        function goToVideo() {
          const videoUrl = document.getElementById('video-input').value;
          if (videoUrl) {
            window.location.href = 'https://jx.playerjy.com/?url=' + encodeURIComponent(videoUrl);
          } else {
            alert('Video');
          }
        }
        
        function goToTranslate() {
          const translateQuery = document.getElementById('translate-input').value;
          if (translateQuery) {
            window.location.href = 'https://cf.haoss.eu.org/oooo/https/translate.google.com?sl=auto&tl=zh-CN&text=' + encodeURIComponent(translateQuery);
          } else {
            alert('Translate');
          }
        }

        function goToStartpage() {
          const startpageQuery = document.getElementById('startpage-input').value;
          if (startpageQuery) {
            window.location.href = 'https://cf.haoss.eu.org/oooo/https/www.startpage.com/search?q=' + encodeURIComponent(startpageQuery);
          } else {
            alert('Startpage');
          }
        }

        function goToContent() {
          const contentQuery = document.getElementById('content-input').value;
          if (contentQuery) {
            window.location.href = 'https://fan3.515519.xyz/Alvin9999/https/www.youtube.com/results?search_query=' + encodeURIComponent(contentQuery);
          } else {
            alert('Content');
          }
        }
      </script>
    </body>
    </html>
  `;

  return new Response(html, {
    headers: { 'Content-Type': 'text/html' },
  });
}
