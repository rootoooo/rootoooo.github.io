addEventListener('fetch', event => {
  event.respondWith(handleRequest(event.request));
});

async function handleRequest(request) {
  const ip = request.headers.get('CF-Connecting-IP') || '未知';
  const country = request.headers.get('CF-IPCountry') || '未知';
  const EconomyURL = 'https://zh.tradingeconomics.com';
  const PanSouURL = 'https://so.252035.xyz';
  const TranslateURL = 'https://cf.haoss.eu.org/oooo/https/translate.google.com/?hl=zh-cn&sl=auto&tl=zh-CN&op=translate';

  const html = `
    <!DOCTYPE html>
    <html lang="zh">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>皮皮虾</title>
      <link rel="icon" type="image/jpg" href="https://haoss.eu.org/ppx.jpg" />
      <style>
        body {
          font-family: Arial, sans-serif;
          text-align: center;
          margin: 0;
          padding: 0;
          height: 100vh;
          display: flex;
          justify-content: center;
          align-items: center;
          background: url('https://cf.haoss.eu.org/oooo/https/raw.githubusercontent.com/rootoooo/rootoooo.github.io/refs/heads/main/ppx01.jpg') no-repeat center center fixed;
          background-size: cover;
        }
        .container {
          background: rgba(255, 255, 255, 0.7);
          padding: 20px;
          border-radius: 10px;
          box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
          text-align: center;
        }
        h1 {
          font-size: 24px;
          margin-bottom: 20px;
        }
        p {
          font-size: 18px;
          margin: 10px 0;
        }
        .link-container {
          margin-top: 10px;
          display: flex;
          justify-content: center;
          gap: 15px;
          flex-wrap: wrap;
        }
        .link-container a {
          font-size: 16px;
          color: blue;
          text-decoration: underline;
          cursor: pointer;
        }
        .input-container,
        .video-input-container,
        .startpage-input-container,
        .wikipedia-input-container {
          margin-top: 20px;
        }
        input {
          font-size: 16px;
          padding: 8px;
          width: 200px;
          margin-right: 10px;
        }
        button {
          font-size: 16px;
          padding: 8px 16px;
          cursor: pointer;
        }
      </style>
    </head>
    <body>
      <div class="container">
        <h1>There is everything in dreams</h1>
        <p><strong>IP:</strong> ${ip}</p>
        <p><strong>Nation:</strong> ${country}</p>
        <div class="link-container">
          <a href="${EconomyURL}" class="details-link">Economy</a>
          <a href="${PanSouURL}" class="details-link">PanSou</a>
          <a href="${TranslateURL}" class="details-link">Translate</a>
        </div>

        <div class="input-container">
          <input type="text" id="ip-input" placeholder="IP Port" />
          <button onclick="goToPing()">Go</button>
        </div>

        <div class="video-input-container">
          <input type="text" id="video-input" placeholder="Video" />
          <button onclick="goToVideo()">Go</button>
        </div>

        <div class="startpage-input-container">
          <input type="text" id="startpage-input" placeholder="Startpage" />
          <button onclick="goToStartpage()">Go</button>
        </div>

        <div class="wikipedia-input-container">
          <input type="text" id="wikipedia-input" placeholder="Wikipedia" />
          <button onclick="goToWikipedia()">Go</button>
        </div>
      </div>

      <script>
        function goToPing() {
          const ip = document.getElementById('ip-input').value;
          if (ip) {
            window.location.href = 'https://ping.pe/' + ip;
          } else {
            alert('IP Port');
          }
        }

        function goToVideo() {
          const videoUrl = document.getElementById('video-input').value;
          if (videoUrl) {
            window.location.href = 'https://jx.playerjy.com/?url=' + encodeURIComponent(videoUrl);
          } else {
            alert('Video');
          }
        }
        
        function goToStartpage() {
          const startpageQuery = document.getElementById('startpage-input').value;
          if (startpageQuery) {
            window.location.href = 'https://cf.haoss.eu.org/oooo/https/www.startpage.com/search?q=' + encodeURIComponent(startpageQuery);
          } else {
            alert('Startpage');
          }
        }

        function goToWikipedia() {
          const query = document.getElementById('wikipedia-input').value;
          if (query) {
            window.location.href = 'https://cf.haoss.eu.org/oooo/https/zh.wikipedia.org/wiki/' + encodeURIComponent(query);
          } else {
            alert('Wikipedia');
          }
        }
      </script>
    </body>
    </html>
  `;

  return new Response(html, {
    headers: { 'Content-Type': 'text/html' },
  });
}
